LABEL rescarch_fast
TEXT HELP
Boot the RescArch Live Environment on BIOS in fast mode.
It loads the needed file directly from the usb drive, it makes the boot very faster, but makes it run slower.
ENDTEXT
MENU LABEL RescArch fast boot
LINUX /%INSTALL_DIR%/boot/%ARCH%/vmlinuz-linux
INITRD /%INSTALL_DIR%/boot/%ARCH%/initramfs-linux.img
APPEND %RA_BOOT_CMDLINE% copytoram=n
